# --- Read by the app today (M3 — auth) ---
# Base URL of apps/api, no trailing slash and no /api/v1 suffix (the auth
# client appends that). Defaults to http://localhost:4000 if unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
