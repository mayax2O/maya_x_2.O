# --- Read by the app today (M4 — Admin Dashboard & Talent Management) ---
# Base URL of apps/api, no trailing slash and no /api/v1 suffix (the admin
# API client appends that). Defaults to http://localhost:4000 if unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
